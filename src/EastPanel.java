import javax.swing.*;
import java.awt.*;

public class EastPanel  extends JPanel {
    public EastPanel() {
        setBackground(Color.GRAY);
        setPreferredSize(new Dimension(200, 0));

    }
}
